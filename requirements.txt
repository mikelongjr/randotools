# RealESRGAN Upscaler - Python dependencies
# Install with:  pip install -r requirements.txt
#
# For GPU acceleration, install the appropriate PyTorch build:
#   NVIDIA (CUDA 12.1): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   AMD (ROCm 6.0):     pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.0
#   CPU only:           pip install torch torchvision

# Core ML
torch>=2.0.0
torchvision>=0.15.0

# GUI
PyQt6>=6.4.0

# Image processing
Pillow>=9.0.0
opencv-python>=4.7.0

# RealESRGAN model
py_real_esrgan>=0.0.3

# huggingface_hub 0.16+ removed cached_download which py_real_esrgan requires
huggingface_hub<0.16.0

# BasicSR (optional – enables additional model variants: anime_6B, general-x4v3)
# Well-known issue: basicsr uses `git describe` to detect its own version at build
# time.  Outside a git checkout this fails.  Workaround:
#   SETUPTOOLS_SCM_PRETEND_VERSION=1.4.2 CUDA_VISIBLE_DEVICES='' pip install basicsr
# Uncomment the line below only after installing build dependencies:
#   sudo dnf install gcc-c++ python3-devel
# basicsr>=1.4.2

# Progress and CLI utilities
tqdm>=4.64.0

# Configuration
# (json and pathlib are from the standard library)

# YAML config support (optional)
# PyYAML>=6.0
